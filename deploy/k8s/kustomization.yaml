apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: p01

resources:
  - deployment-idp.yaml
  - deployment-admin.yaml
  - service-idp.yaml
  - service-admin.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: portfolio-integration
    includeSelectors: false
