# Local only. Compose reads this file; never commit it.

POSTGRES_USER=idp
POSTGRES_PASSWORD=change-me-locally
POSTGRES_DB=idp
POSTGRES_PORT=5432

# Used by the admin UI BFF. Do not reuse outside this machine.
IDENTITY_ADMIN_TOKEN=change-me-admin-token

# Optional local demo login (seeded on IdP boot when set).
IDENTITY_SEED_DEMO_EMAIL=demo@example.test
IDENTITY_SEED_DEMO_PASSWORD=demo-pass-change-me
IDENTITY_SEED_DEMO_NAME=Demo User
