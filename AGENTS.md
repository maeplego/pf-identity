# P01 pf-identity

このディレクトリは製品コードです。実装前にワークスペース（`project`）側を読む。

1. `project/portfolio-plan/instructions.md` または `project/AGENTS.md`
2. `project/identity-platform/AGENTS.md`
3. `project/portfolio-plan/identity-platform/DESIGN.md`
4. `project/portfolio-idea/14-oauth-oidc-identity-provider.md`
5. `project/identity-platform/chat-context/` をファイル名順

テスト: `go test ./...` を `apps/server` で実行し、通ってからこのリポジトリにコミットする。
